# Supabase (browser; anon key only — never commit service role)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Dev only: where Vite proxies /api (your Express API). Default 3001 matches api-server.
# VITE_API_PROXY_TARGET=http://localhost:5000
